class Clock {
    constructor(h, m, s, ms, aInit) {
        this.hour = h
        this.min = m
        this.sec = s
        this.milsec = ms
        this.a = aInit
    }
    drawHours(i, hour) {
    }
    drawMins(i, min) {
    }
    drawSecs(i, sec) {
    }
    drawMilSecs(i, msec) {
    }
}
